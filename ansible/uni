---
- hosts: host
  vars:
    vm_name: fedora-lxqt
    share_folder: "/home/me/src"
  roles:
  # - VirtioFS/share folder
  - Hyperion/start VM

- hosts: guest
  vars:
    mount_on_boot: true
  roles:
  - VirtioFS/mount
